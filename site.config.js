/* ============================================================
   The one place to edit shared settings.

   SITE_URL is where the QR code sends people. After you publish the
   site (see README), put the real address here and run:

       node tools/make-qr.mjs "https://your-real-url/"

   ...which rewrites this file AND regenerates assets/qr.svg together,
   so the code and the caption can never disagree.
   ============================================================ */

const SITE_URL = "https://example.com/halo";
